#include <iostream>
#include <cmath>

using namespace std;

int main(){
    
    int x;
    cin >> x;

    cout << "The next number for the number "<< x <<" is " << x + 1 << "." << endl;
    cout << "The previous number for the number "<< x <<" is " << x - 1 << ".";

    return 0;
}